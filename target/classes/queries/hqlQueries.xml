<?xml version='1.0' encoding='utf-8'?>
<queries>
	<query name="pdoCte.getAllActivesByCteId">
		<![CDATA[
				select
					cte.idProducto, cte.nombreProducto, cte.fechaCreacion, cte.estado
				from
					producto cte LEFT JOIN cte.clientesProducto cat LEFT JOIN cliente cli
				where
					cat.idCliente = cli.idCliente
					and cat.idCliente = ?1
					and cte.estado = ?2
				order by
					cte.nombreProducto asc
		]]>
	</query>
</queries>